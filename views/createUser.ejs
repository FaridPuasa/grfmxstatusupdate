<!DOCTYPE html>
<html>

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Create User - Go Rush</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">
    <link rel="stylesheet" href="/css/mobile-center.css">
    <!-- Cropper.js CSS -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/cropperjs/1.5.13/cropper.min.css">
    <link rel="stylesheet" href="/css/userForm.css">
    <link rel="icon" type="image/png" href="/images/smallgorushlogo.png">
</head>

<body>
    <%- include('partials/navbar') %>

    <div class="container mt-4">
        <div class="row justify-content-center">
            <div class="col-md-8">
                <div class="card">
                    <div class="card-header">
                        <h2><i class="bi bi-person-plus"></i> Create New User</h2>
                    </div>
                    <div class="card-body">
                        <% if (typeof errors !=='undefined' && errors.length> 0) { %>
                            <div class="alert alert-danger alert-dismissible fade show" role="alert">
                                <strong><i class="bi bi-exclamation-triangle-fill"></i> Errors:</strong>
                                <ul class="mb-0 mt-2">
                                    <% errors.forEach(function(error) { %>
                                        <li>
                                            <%= error.msg %>
                                        </li>
                                        <% }); %>
                                </ul>
                                <button type="button" class="btn-close" data-bs-dismiss="alert"
                                    aria-label="Close"></button>
                            </div>
                            <% } %>
                                <% const fd = (typeof formData !=='undefined' && formData) ? formData : {} %>

                                <form action="/createUser" method="post" id="userForm">
                                    <div class="form-group">
                                        <label class="form-label">Role <span class="text-danger">*</span></label>
                                        <select name="role" id="role" class="form-select" required>
                                            <option value="">Select Role</option>
                                            <option value="admin" <%=fd.role==='admin' ? 'selected' : '' %>>Admin</option>
                                            <option value="supervisor" <%=fd.role==='supervisor' ? 'selected' : '' %>>Supervisor</option>
                                            <option value="manager" <%=fd.role==='manager' ? 'selected' : '' %>>Manager</option>
                                            <option value="cs" <%=fd.role==='cs' ? 'selected' : '' %>>CS</option>
                                            <option value="warehouse" <%=fd.role==='warehouse' ? 'selected' : '' %>>Warehouse</option>
                                            <option value="finance" <%=fd.role==='finance' ? 'selected' : '' %>>Finance</option>
                                            <option value="moh" <%=fd.role==='moh' ? 'selected' : '' %>>MOH</option>
                                            <option value="dispatcher" <%=fd.role==='dispatcher' ? 'selected' : '' %>>Dispatcher</option>
                                            <option value="freelancer" <%=fd.role==='freelancer' ? 'selected' : '' %>>Freelancer</option>
                                        </select>
                                    </div>

                                    <div id="regularFields" style="display: none;">
                                        <div class="form-group">
                                            <label class="form-label">Full Name</label>
                                            <input type="text" name="fullName" class="form-control"
                                                value="<%= fd.fullName || '' %>" placeholder="Enter full name">
                                        </div>

                                        <div class="form-group">
                                            <label class="form-label">Nickname <span
                                                    class="text-danger">*</span></label>
                                            <input type="text" name="name" class="form-control" required
                                                value="<%= fd.name || '' %>" placeholder="Enter nickname">
                                        </div>

                                        <div class="form-group">
                                            <label class="form-label">Email <span class="text-danger"
                                                    id="emailRequired">*</span></label>
                                            <input type="email" name="email" class="form-control" id="email"
                                                value="<%= fd.email || '' %>" placeholder="Enter email address">
                                            <small class="text-muted" id="emailHint" style="display: none;">Email is
                                                optional for Freelancer/Dispatcher roles</small>
                                        </div>

                                        <div class="form-group">
                                            <label class="form-label">Password <span class="text-danger"
                                                    id="passwordRequired">*</span></label>
                                            <input type="password" name="password" class="form-control" id="password"
                                                placeholder="Enter password (min 6 characters)">
                                            <small class="text-muted" id="passwordHint" style="display: none;">Password
                                                is optional for Freelancer/Dispatcher roles</small>
                                        </div>

                                        <div class="form-group">
                                            <label class="form-label">IC No</label>
                                            <input type="text" name="icNum" class="form-control"
                                                value="<%= fd.icNum || '' %>" placeholder="Enter IC number">
                                        </div>

                                        <div class="form-group">
                                            <label class="form-label">Company</label>
                                            <select name="company" class="form-select">
                                                <option value="Gorush" <%=fd.company==='Gorush' ? 'selected' : '' %>>Go Rush</option>
                                                <option value="Globex" <%=fd.company==='Globex' ? 'selected' : '' %>>Globex</option>
                                                <option value="Rbskyshop" <%=fd.company==='Rbskyshop' ? 'selected' : '' %>>RBSkyshop</option>
                                                <option value="Others" <%=fd.company==='Others' ? 'selected' : '' %>>Others</option>
                                            </select>
                                            <small class="text-muted">Select the company this user belongs to
                                                (optional)</small>
                                        </div>

                                        <div class="form-group">
                                            <label class="form-label">Job Position <span
                                                    class="text-danger">*</span></label>
                                            <input type="text" name="jobPosition" class="form-control" required
                                                value="<%= fd.jobPosition || '' %>" placeholder="Enter job position">
                                        </div>

                                        <div class="form-group">
                                            <label class="form-label">Status <span class="text-danger">*</span></label>
                                            <select name="status" class="form-select" required>
                                                <option value="Active" <%=fd.status==='Active' || !fd.status ? 'selected' : '' %>>Active</option>
                                                <option value="Inactive" <%=fd.status==='Inactive' ? 'selected' : '' %>>Inactive</option>
                                            </select>
                                        </div>

                                        <div class="form-group">
                                            <label class="form-label">Profile Picture</label>
                                            <div class="image-upload-area"
                                                onclick="document.getElementById('profilePictureInput').click()">
                                                <i class="bi bi-cloud-arrow-up" style="color: #2078BE; font-size: 2rem;"></i>
                                                <p class="mt-2">Click to upload profile picture</p>
                                                <small class="text-muted">Supported formats: JPG, PNG, GIF (Click to
                                                    crop)</small>
                                            </div>
                                            <input type="file" id="profilePictureInput" accept="image/*"
                                                style="display: none;">
                                            <input type="hidden" name="profilePicture" id="profilePicture">
                                            <div id="profileImageContainer"></div>
                                        </div>
                                    </div>

                                    <div class="form-group mt-4">
                                        <button type="submit" class="btn-submit">
                                            <i class="bi bi-save"></i> Create User
                                        </button>
                                    </div>
                                </form>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <!-- Cropper Modal -->
    <div id="cropperModal" class="cropper-modal">
        <div class="cropper-container-box">
            <img id="cropperImage" class="cropper-image" alt="Crop Image">
            <div class="cropper-buttons">
                <button class="btn-crop" onclick="cropImage()">Crop & Save</button>
                <button class="btn-cancel" onclick="closeCropper()">Cancel</button>
            </div>
        </div>
    </div>

    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
    <!-- Cropper.js JS -->
    <script src="https://cdnjs.cloudflare.com/ajax/libs/cropperjs/1.5.13/cropper.min.js"></script>
    <script src="/js/userFormCropper.js"></script>

    <script>
        // Show/hide fields based on role
        document.getElementById('role').addEventListener('change', function () {
            const role = this.value;
            const regularFields = document.getElementById('regularFields');
            const emailField = document.getElementById('email');
            const passwordField = document.getElementById('password');
            const emailRequired = document.getElementById('emailRequired');
            const passwordRequired = document.getElementById('passwordRequired');
            const emailHint = document.getElementById('emailHint');
            const passwordHint = document.getElementById('passwordHint');

            if (role === 'freelancer' || role === 'dispatcher') {
                regularFields.style.display = 'block';
                emailField.removeAttribute('required');
                passwordField.removeAttribute('required');
                emailRequired.style.display = 'none';
                passwordRequired.style.display = 'none';
                if (emailHint) emailHint.style.display = 'block';
                if (passwordHint) passwordHint.style.display = 'block';
                emailField.placeholder = "Email (optional for Freelancer/Dispatcher)";
                passwordField.placeholder = "Password (optional for Freelancer/Dispatcher)";
            } else if (role && role !== '') {
                regularFields.style.display = 'block';
                emailField.setAttribute('required', 'required');
                passwordField.setAttribute('required', 'required');
                emailRequired.style.display = 'inline';
                passwordRequired.style.display = 'inline';
                if (emailHint) emailHint.style.display = 'none';
                if (passwordHint) passwordHint.style.display = 'none';
                emailField.placeholder = "Enter email address";
                passwordField.placeholder = "Enter password (min 6 characters)";
            } else {
                regularFields.style.display = 'none';
            }
        });

        // Profile Picture upload with cropper
        const profileInput = document.getElementById('profilePictureInput');
        if (profileInput) {
            profileInput.addEventListener('change', function (e) {
                const file = e.target.files[0];
                if (file) {
                    openCropper(file);
                    profileInput.value = ''; // Clear input
                }
            });
        }

        // Trigger role change on page load
        if (document.getElementById('role').value) {
            document.getElementById('role').dispatchEvent(new Event('change'));
        }
    </script>
</body>

</html>